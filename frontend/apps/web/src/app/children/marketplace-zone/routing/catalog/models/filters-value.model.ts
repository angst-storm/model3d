
export interface IFiltersValueModel {
    color: { value: boolean, id: string }[],
    format: { value: boolean, id: string }[],
    form: { value: boolean, id: string }[],
    material: { value: boolean, id: string }[],
    render: { value: boolean, id: string }[],
    style: { value: boolean, id: string }[],
}
